/**
 * Edit these two lines after running `tagada-init`.
 *
 *   npx -p @tagadapay/node-sdk tagada-init
 *
 * The CLI prints a `VITE_STORE_ID=store_xxxxxx` line — copy the id here.
 */
export const STORE_ID = 'store_xxxxxx';
export const ENVIRONMENT = 'production'; // or 'development'

/** Brand identity — change to taste. */
export const BRAND = {
  name: 'NORTH',
  tagline: 'Considered apparel for every season.',
};
